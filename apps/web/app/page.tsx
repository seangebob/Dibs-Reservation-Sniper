/**
 * Placeholder home route.
 *
 * Task 8 scaffolds the app and its client-identity / API plumbing only; the
 * real prompt page (with the approved visual direction) is built in Task 9 and
 * replaces this component. It renders a minimal, dependency-free page so
 * `next build` and a local `next dev` both succeed against the scaffold.
 */
export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: 48 }}>
      <h1>Dibs</h1>
      <p>Frontend scaffold is up. The prompt page arrives in Task 9.</p>
    </main>
  );
}
