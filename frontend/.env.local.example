# Base URL of the Dibs FastAPI backend. The browser calls this directly, so it
# must be reachable from the visitor's machine (not a docker-internal host).
# Copy this file to `.env.local` for local development.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
